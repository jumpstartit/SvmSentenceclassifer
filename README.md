# Experimenting with SVM classifer for Sentiment Analysis

Master Branch uses the sklearn svm method
svm-linear_classifere branch is python implemntation of svm class implmenting hinge loss post dual representation of svm formula
